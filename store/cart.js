import {CONSTANTS} from "@/utils/constant";

const actions = {
};

const state = () => ({});

const getters = {};

const mutations = {};

export default {
  actions,
  state,
  getters,
  mutations,
};
